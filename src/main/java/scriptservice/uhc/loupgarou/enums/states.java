package scriptservice.uhc.loupgarou.enums;

public enum states {
    WAITING, STARTING, PREGAME, GAME, FINISH
    // waiting = en attente
    // starting = countdown
    // game = en game (apres roles)
    // pregame = en minage (avant roles)
    // finish = victoire d'un camps
}
